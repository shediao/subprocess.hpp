name: CMake on BSD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  # ─────────────────────────────────────────────────────────────────────────
  # FreeBSD build
  # ─────────────────────────────────────────────────────────────────────────
  freebsd-build:
    runs-on: ubuntu-latest
    name: FreeBSD Build
    steps:
      - uses: actions/checkout@v5

      - name: build on FreeBSD vm
        uses: vmactions/freebsd-vm@v1
        with:
          usesh: true
          prepare: |
            pkg install -y cmake git bash
          run: |
            cmake -B build -S . -DCMAKE_BUILD_TYPE=Release -DSUBPROCESS_BUILD_TESTS=ON
            cmake --build build
            ctest --test-dir build --output-on-failure

  # ─────────────────────────────────────────────────────────────────────────
  # NetBSD build
  # ─────────────────────────────────────────────────────────────────────────
  netbsd-build:
    runs-on: ubuntu-latest
    name: NetBSD Build
    steps:
      - uses: actions/checkout@v5

      - name: build on NetBSD vm
        uses: vmactions/netbsd-vm@v1
        with:
          usesh: true
          prepare: |
            set -e
            # Retry up to 5 times to handle transient CDN outages
            for i in $(seq 1 5); do
              pkg_add -U cmake git zip bash gcc13 && break
              echo "pkg_add attempt $i failed, retrying in 10s..."
              sleep 10
            done
          run: |
            cmake -B build -S . -DCMAKE_C_COMPILER=/usr/pkg/gcc13/bin/gcc -DCMAKE_CXX_COMPILER=/usr/pkg/gcc13/bin/g++  -DCMAKE_BUILD_TYPE=Release -DSUBPROCESS_BUILD_TESTS=ON
            cmake --build build
            ctest --test-dir build --output-on-failure

  # ─────────────────────────────────────────────────────────────────────────
  # OpenBSD build
  # ─────────────────────────────────────────────────────────────────────────
  openbsd-build:
    runs-on: ubuntu-latest
    name: OpenBSD Build
    steps:
      - uses: actions/checkout@v5

      - name: build on OpenBSD vm
        uses: vmactions/openbsd-vm@v1
        with:
          usesh: true
          prepare: |
            pkg_add cmake git bash
          run: |
            cmake -B build -S . -DCMAKE_BUILD_TYPE=Release -DSUBPROCESS_BUILD_TESTS=ON
            cmake --build build
            ctest --test-dir build --output-on-failure
